okay
so
basically


idk.

'exit' -> turns off the project and stops the code

'scan udp ports (ip address)'
'scan udp ports: (ip address)'
'scan UDP ports (ip address)'
'scan UDP ports: (ip address)' -> print which udp ports are open to the terminal

'scan tcp ports (ip address)'
'scan tcp ports: (ip address)'
'scan TCP ports (ip address)'
'scan TCP ports: (ip adress)' -> print which tcp ports are open to the terminal

'scan both ports (ip address)'
'scan both ports: (ip address)' -> print which tcp and udp ports are open to the terminal

'ping-sweep (subnet)' -> prints all devices's IPv4 adress which are online to the terminal

'packet-sniff (number)' -> prints the details of each packet (number is how many packets you want to sniff) to the terminal if wanted
